# Contributing

New harness extensions welcome: reuse `@gintasz/neuralyzer-core` for the wipe logic and shared strings, write only the host glue. Build + unit tests must pass (Node 22+); an e2e test is desirable. [MIT](LICENSE).
